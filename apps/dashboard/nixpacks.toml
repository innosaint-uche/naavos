[phases.setup]
nixPkgs = ["nodejs_22"]
cmds = ["npm install -g pnpm@9.15.9"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"

[variables]
NODE_ENV = "production"
PORT = "3000"
